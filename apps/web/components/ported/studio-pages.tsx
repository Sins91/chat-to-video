"use client";

import Link from "next/link";
import { Bot, Check, ChevronRight, Image, Mic2, MoreHorizontal, Plus, Search, Shapes, Sparkles, Users, WandSparkles } from "lucide-react";
import { useState } from "react";

import { dramas, filmSteps, libraryItems } from "./mock-data";
import { StudioHeader } from "./studio-shell";

export function ProjectsPage() {
  return <><StudioHeader title="短剧项目" description="管理你的故事、剧集与发布状态" action="新建短剧" /><div className="p-6 lg:p-9"><div className="mb-6 flex flex-wrap gap-3"><div className="flex h-9 min-w-64 items-center gap-2 rounded-md border bg-white px-3"><Search className="size-4 text-zinc-400" /><input className="w-full bg-transparent text-sm outline-none" placeholder="搜索项目" /></div>{["全部状态", "全部类型", "最近更新"].map((item) => <button type="button" key={item} className="h-9 rounded-md border bg-white px-3 text-sm text-zinc-600">{item}</button>)}</div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{dramas.slice(0, 5).map((drama, index) => <Link href={`/studio/dramas/${drama.id}`} key={drama.id} className="group rounded-md border bg-white p-3"><div className={`relative aspect-video overflow-hidden rounded ${drama.tone === "warm" ? "cover-art-warm" : "cover-art"}`}><span className="absolute right-3 top-3 rounded bg-black/50 px-2 py-1 text-[10px] text-white">{index < 2 ? "制作中" : "草稿"}</span></div><div className="flex items-start gap-3 px-1 pb-2 pt-4"><div className="min-w-0"><h2 className="truncate font-semibold">{drama.title}</h2><p className="mt-1 truncate text-xs text-zinc-500">{drama.subtitle}</p><p className="mt-3 text-[11px] text-zinc-400">{drama.episodes} 集 · 更新于 {index + 1} 天前</p></div><MoreHorizontal className="ml-auto size-4 text-zinc-400" /></div></Link>)}<button type="button" className="grid min-h-64 place-items-center rounded-md border border-dashed bg-white text-center text-zinc-500"><span><Plus className="mx-auto size-6" /><span className="mt-3 block text-sm">创建新项目</span></span></button></div></div></>;
}

export function DramaAdminPage({ dramaId }: { readonly dramaId: string }) {
  const drama = dramas.find((item) => item.id === dramaId) ?? dramas[0];
  return <><StudioHeader title={drama.title} description="项目详情与剧集管理" action="添加剧集" /><div className="p-6 lg:p-9"><section className="grid gap-6 rounded-md border bg-white p-5 lg:grid-cols-[220px_1fr]"><div className={`aspect-[3/4] rounded ${drama.tone === "warm" ? "cover-art-warm" : "cover-art"}`} /><div><div className="flex items-center gap-2"><span className="rounded bg-blue-50 px-2 py-1 text-xs text-blue-700">制作中</span><span className="rounded bg-zinc-100 px-2 py-1 text-xs text-zinc-600">{drama.genre}</span></div><h2 className="mt-4 text-2xl font-semibold">{drama.title}</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-zinc-500">{drama.subtitle}。这是用于保留原项目详情页结构的静态内容，项目元数据不会从后端读取。</p><div className="mt-6 grid max-w-xl grid-cols-3 gap-4 text-sm"><div><p className="text-xs text-zinc-400">画幅</p><p className="mt-1 font-medium">16:9</p></div><div><p className="text-xs text-zinc-400">风格</p><p className="mt-1 font-medium">电影写实</p></div><div><p className="text-xs text-zinc-400">视频模式</p><p className="mt-1 font-medium">分镜图驱动</p></div></div></div></section><section className="mt-8"><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-semibold">剧集</h2><span className="text-xs text-zinc-400">共 {drama.episodes} 集</span></div><div className="space-y-3">{Array.from({ length: Math.min(drama.episodes, 6) }, (_, index) => <Link href={`/studio/dramas/${drama.id}/episodes/${index + 1}/film`} key={index} className="flex items-center gap-4 rounded-md border bg-white p-4"><span className="grid size-11 place-items-center rounded bg-zinc-100 text-sm font-semibold">{String(index + 1).padStart(2, "0")}</span><div><h3 className="text-sm font-medium">第 {index + 1} 集 · {index === 0 ? "雨夜的来信" : "尚未命名"}</h3><p className="mt-1 text-xs text-zinc-400">{index === 0 ? "分镜制作中" : "等待开始"}</p></div><ChevronRight className="ml-auto size-4 text-zinc-400" /></Link>)}</div></section></div></>;
}

export function FilmWorkspace() {
  const [step, setStep] = useState(0);
  const selectedStep = filmSteps[step] ?? filmSteps[0];
  return <div className="flex h-dvh flex-col overflow-hidden bg-[#f7f7f8]"><header className="flex h-14 shrink-0 items-center border-b bg-white px-5"><Link href="/studio/dramas/1" className="text-sm text-zinc-500">短剧项目 / 午夜来信 /</Link><span className="ml-2 text-sm font-medium">第 1 集</span><div className="ml-auto flex gap-2"><button type="button" className="h-8 rounded-md border px-3 text-xs">保存草稿</button><button type="button" className="inline-flex h-8 items-center gap-2 rounded-md bg-black px-3 text-xs text-white"><WandSparkles className="size-3" />自动生成</button></div></header><div className="flex min-h-0 flex-1"><aside className="w-52 shrink-0 overflow-auto border-r bg-white p-3"><p className="px-3 py-3 text-xs font-medium text-zinc-400">制作流程</p>{filmSteps.map((label, index) => <button type="button" key={label} onClick={() => setStep(index)} className={`mb-1 flex h-10 w-full items-center gap-3 rounded-md px-3 text-left text-sm ${step === index ? "bg-black text-white" : "text-zinc-600 hover:bg-zinc-100"}`}><span className={`grid size-5 place-items-center rounded-full border text-[10px] ${index < step ? "border-emerald-500 bg-emerald-500 text-white" : ""}`}>{index < step ? <Check className="size-3" /> : index + 1}</span>{label}</button>)}</aside><main className="min-w-0 flex-1 overflow-auto p-6 lg:p-9"><div className="mx-auto max-w-6xl"><div className="flex items-end justify-between"><div><p className="text-xs text-zinc-400">STEP {step + 1} / 8</p><h1 className="mt-2 text-2xl font-semibold">{selectedStep}</h1><p className="mt-2 text-sm text-zinc-500">{step === 0 ? "整理故事梗概与本集剧本" : step < 4 ? "管理本集可复用的视觉素材" : step < 6 ? "将故事拆解为连续镜头" : "生成视频并完成最终合成"}</p></div><button type="button" className="inline-flex h-9 items-center gap-2 rounded-md bg-gradient-to-r from-fuchsia-600 to-blue-500 px-4 text-sm text-white"><Sparkles className="size-4" />生成{selectedStep}</button></div>{step === 0 ? <div className="mt-7 grid gap-5 lg:grid-cols-[1fr_1.5fr]"><div className="rounded-md border bg-white p-5"><h2 className="font-medium">故事梗概</h2><textarea className="mt-4 min-h-64 w-full resize-none rounded-md border bg-zinc-50 p-4 text-sm leading-7 outline-none" defaultValue="林夏在一个雨夜收到来自十年后的信，信中警告她不要登上第二天的末班车。" /></div><div className="rounded-md border bg-white p-5"><h2 className="font-medium">本集剧本</h2><textarea className="mt-4 min-h-64 w-full resize-none rounded-md border bg-zinc-50 p-4 font-mono text-xs leading-6 outline-none" defaultValue={"01 旧街 / 夜 / 外\n\n雨水沿着霓虹灯牌落下。林夏停在十三号信箱前。\n\n林夏（旁白）：那封信，写着我的名字，也写着十年后的日期。"} /></div></div> : <div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{Array.from({ length: 6 }, (_, index) => <div key={index} className="rounded-md border bg-white p-3"><div className={`relative aspect-video rounded ${index % 2 ? "cover-art-warm" : "cover-art"}`}><span className="absolute left-3 top-3 rounded bg-black/45 px-2 py-1 text-[10px] text-white">{selectedStep} {index + 1}</span></div><div className="px-1 py-3"><h2 className="text-sm font-medium">{step === 1 ? "角色 · 林夏" : step === 2 ? "场景 · 雨夜旧街" : step === 3 ? "道具 · 未寄出的信" : `镜头 ${String(index + 1).padStart(2, "0")}`}</h2><p className="mt-1 truncate text-xs text-zinc-400">点击卡片可编辑提示词与参考信息</p></div></div>)}</div>}</div></main></div></div>;
}

export function LibraryPage({ kind }: { readonly kind: "角色" | "场景" | "道具" | "声音" }) {
  const iconMap = { 角色: Users, 场景: Image, 道具: Shapes, 声音: Mic2 };
  const Icon = iconMap[kind];
  return <><StudioHeader title={`${kind}库`} description={`跨项目复用你的${kind}资产`} action={`添加${kind}`} /><div className="p-6 lg:p-9"><div className="mb-6 flex h-9 max-w-xs items-center gap-2 rounded-md border bg-white px-3"><Search className="size-4 text-zinc-400" /><input className="w-full bg-transparent text-sm outline-none" placeholder={`搜索${kind}`} /></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{libraryItems.map((item, index) => <div key={item} className="rounded-md border bg-white p-3"><div className={`grid aspect-video place-items-center rounded ${index % 2 ? "cover-art-warm" : "cover-art"}`}><Icon className="size-8 text-white/80" /></div><div className="px-1 py-3"><h2 className="text-sm font-medium">{item}</h2><p className="mt-1 text-xs text-zinc-400">已用于 {index + 1} 个项目</p></div></div>)}</div></div></>;
}

export function AIConfigPage() {
  return <><StudioHeader title="AI 配置" description="配置创作流程中使用的模型服务" action="添加服务" /><div className="mx-auto max-w-5xl p-6 lg:p-9"><div className="grid gap-4 md:grid-cols-2">{[["文本与脚本", "DeepSeek V3", "已配置"], ["图像生成", "通义万相", "待配置"], ["视频生成", "Seedance", "待配置"], ["语音合成", "CosyVoice", "已配置"]].map(([name, model, status]) => <div key={name} className="rounded-md border bg-white p-5"><div className="flex items-center"><span className="grid size-10 place-items-center rounded-md bg-zinc-100"><Bot className="size-5" /></span><div className="ml-3"><h2 className="text-sm font-medium">{name}</h2><p className="mt-1 text-xs text-zinc-400">{model}</p></div><span className={`ml-auto rounded px-2 py-1 text-[10px] ${status === "已配置" ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700"}`}>{status}</span></div><div className="mt-5 border-t pt-4"><label className="text-xs text-zinc-500">API Key<input type="password" className="mt-2 h-9 w-full rounded-md border px-3 text-sm" defaultValue={status === "已配置" ? "sk-placeholder-key" : ""} placeholder="仅作 UI 展示" /></label><button type="button" className="mt-3 h-8 rounded-md border px-3 text-xs">测试连接</button></div></div>)}</div></div></>;
}

export function PricingPage() {
  return <><StudioHeader title="套餐与价格" description="选择适合你的创作额度" action="查看积分" /><div className="mx-auto max-w-6xl p-6 py-12"><div className="text-center"><h2 className="text-3xl font-semibold">从一个故事开始</h2><p className="mt-3 text-sm text-zinc-500">所有价格均为静态展示，不会发起支付。</p></div><div className="mt-10 grid gap-4 md:grid-cols-3">{[["体验版", "¥0", "每月 100 积分"], ["创作者", "¥99", "每月 5,000 积分"], ["专业版", "¥299", "每月 20,000 积分"]].map(([name, price, quota], index) => <div key={name} className={`rounded-md border bg-white p-6 ${index === 1 ? "ring-2 ring-black" : ""}`}><p className="text-sm font-medium">{name}</p><p className="mt-5 text-4xl font-semibold">{price}<span className="text-sm font-normal text-zinc-400"> / 月</span></p><p className="mt-3 text-sm text-zinc-500">{quota}</p><button type="button" className={`mt-7 h-10 w-full rounded-md text-sm ${index === 1 ? "bg-black text-white" : "border"}`}>选择套餐</button><ul className="mt-6 space-y-3 text-sm text-zinc-600">{["脚本与分镜生成", "高清图片生成", "视频合成与导出"].map((item) => <li key={item} className="flex gap-2"><Check className="size-4 text-emerald-600" />{item}</li>)}</ul></div>)}</div></div></>;
}

export function SettingsPage({ section }: { readonly section: "账号设置" | "团队管理" | "积分明细" }) {
  return <><StudioHeader title={section} description="静态设置页面" action="保存" /><div className="mx-auto max-w-4xl p-6 lg:p-9">{section === "积分明细" ? <><div className="grid gap-4 sm:grid-cols-3">{[["当前积分", "2,480"], ["本月消耗", "1,260"], ["即将过期", "120"]].map(([label, value]) => <div key={label} className="rounded-md border bg-white p-5"><p className="text-xs text-zinc-400">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>)}</div><div className="mt-6 rounded-md border bg-white"><div className="border-b p-4 font-medium">消耗记录</div>{["分镜图生成", "视频片段生成", "脚本优化"].map((item, index) => <div key={item} className="flex border-b p-4 text-sm last:border-0"><span>{item}</span><span className="ml-auto text-zinc-400">-{(index + 1) * 20} 积分</span></div>)}</div></> : section === "团队管理" ? <div className="rounded-md border bg-white"><div className="flex items-center border-b p-5"><div><h2 className="font-medium">Demo Studio</h2><p className="mt-1 text-xs text-zinc-400">3 位成员</p></div><button className="ml-auto h-9 rounded-md bg-black px-4 text-sm text-white" type="button">邀请成员</button></div>{["Demo Creator · 所有者", "林夏 · 编辑", "程述 · 查看者"].map((item) => <div key={item} className="flex items-center gap-3 border-b p-4 last:border-0"><span className="grid size-9 place-items-center rounded-full bg-zinc-100 text-xs">{item.slice(0, 1)}</span><span className="text-sm">{item}</span><MoreHorizontal className="ml-auto size-4 text-zinc-400" /></div>)}</div> : <div className="space-y-5 rounded-md border bg-white p-6"><div className="flex items-center gap-4"><span className="grid size-16 place-items-center rounded-full bg-gradient-to-br from-fuchsia-500 to-blue-500 font-bold text-white">DE</span><button type="button" className="h-9 rounded-md border px-3 text-sm">更换头像</button></div><label className="block text-sm">显示名称<input className="mt-2 h-10 w-full rounded-md border px-3" defaultValue="Demo Creator" /></label><label className="block text-sm">邮箱<input type="email" className="mt-2 h-10 w-full rounded-md border bg-zinc-50 px-3" defaultValue="demo@example.com" readOnly /></label><label className="block text-sm">个人简介<textarea className="mt-2 min-h-24 w-full rounded-md border p-3" defaultValue="用 AI 讲故事。" /></label></div>}</div></>;
}

export function InvitePage({ code }: { readonly code: string }) {
  return <div className="grid min-h-dvh place-items-center bg-zinc-100 p-5"><div className="w-full max-w-md rounded-md border bg-white p-8 text-center"><span className="mx-auto grid size-12 place-items-center rounded-md bg-black text-white"><Users className="size-5" /></span><h1 className="mt-5 text-xl font-semibold">加入 Demo Studio</h1><p className="mt-2 text-sm leading-6 text-zinc-500">你收到了一份团队邀请。邀请码：{code}</p><button type="button" className="mt-7 h-10 w-full rounded-md bg-black text-sm text-white">接受邀请</button><Link href="/studio" className="mt-4 block text-xs text-zinc-400">暂不加入，返回工作台</Link></div></div>;
}
