{
  "name": "chat-to-video",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=26.7.0 <27",
    "pnpm": ">=11.20.0 <12"
  },
  "scripts": {
    "preinstall": "node scripts/enforce-pnpm.cjs",
    "package-manager:check": "node scripts/enforce-pnpm.cjs",
    "deps:install:cn": "node scripts/install-with-registry-fallback.cjs",
    "db:seed:history": "pnpm --filter @chat-to-video/database db:seed:history",
    "sdk:docs:ai": "node scripts/search-sdk-docs.cjs ai",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:chat": "node scripts/run-chat-dev.cjs",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "pnpm run test:scripts && turbo run test",
    "test:scripts": "node --test scripts/*.test.cjs",
    "test:connectivity:prepare": "pnpm --filter @chat-to-video/contracts build",
    "test:connectivity": "pnpm run test:connectivity:prepare && node scripts/video-connectivity-test.cjs preflight",
    "test:connectivity:planning": "pnpm run test:connectivity:prepare && node scripts/video-connectivity-test.cjs planning",
    "test:connectivity:prepaid": "pnpm run test:connectivity:planning",
    "test:connectivity:paid": "pnpm run test:connectivity:prepare && node scripts/video-connectivity-test.cjs paid",
    "test:connectivity:image:upload": "pnpm run test:connectivity:prepare && node scripts/video-connectivity-test.cjs image:upload",
    "test:connectivity:image:planning": "pnpm run test:connectivity:prepare && node scripts/video-connectivity-test.cjs image:planning",
    "test:connectivity:image:paid": "pnpm run test:connectivity:prepare && node scripts/video-connectivity-test.cjs image:paid",
    "test:templates:planning:prepare": "pnpm --filter @chat-to-video/api build",
    "test:templates:planning": "pnpm run test:templates:planning:prepare && node scripts/template-prompt-planning-test.cjs planning",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.2.0",
    "eslint": "10.8.1",
    "globals": "17.7.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
